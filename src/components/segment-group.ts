'use client';

import { styled } from '@/styled-system/jsx';
import { SegmentGroup as parts } from '@ark-ui/react';

export const SegmentGroup = styled(parts.Root);
export const SegmentGroupItem = styled(parts.Item);
export const SegmentGroupLabel = styled(parts.Label);
export const SegmentGroupItemText = styled(parts.ItemText);
export const SegmentGroupItemControl = styled(parts.ItemControl);
export const SegmentGroupIndicator = styled(parts.Indicator);
